# Persönliche Programmdaten - NIEMALS committen.
# Hier liegen der verschlüsselte Schlüsselspeicher, die eigene Chat-ID,
# die Freundesliste und alle empfangenen Dateien.
vp4_daten/

# PyInstaller-Build-Ergebnisse
build/
dist/
*.spec

# Python
__pycache__/
*.pyc

# Exportierte Schluesseldateien - die enthalten Schluessel im KLARTEXT.
# Zweite Sicherung: eigentlich landen die im Obsidian-Vault, aber falls
# doch einmal eine im Projektordner auftaucht, darf sie nie ins Repo.
VP4 Schluessel.md
*Schluessel*.md
